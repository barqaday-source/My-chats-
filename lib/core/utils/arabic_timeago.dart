// Arabic timeago messages are handled via timeago package built-in ArMessages
// Usage: timeago.format(dateTime, locale: 'ar')
// Make sure to call: timeago.setLocaleMessages('ar', timeago.ArMessages()); in main()
